import React from 'react';
import { Navbar } from '../Navbar';
import { HeroContainer } from './HeroElements';

const Hero = () => {
    return (
        <HeroContainer>
            <Navbar></Navbar>
        </HeroContainer>
    )
}

export default Hero;